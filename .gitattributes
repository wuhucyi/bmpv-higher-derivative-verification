*.pdf binary
*.nb binary
*.nb.gz binary
*.png binary
*.jpg binary
*.jpeg binary

# NB01 and NB02 are stored as deterministic lossless gzip objects below the
# ordinary GitHub per-file limit; Git LFS is not required.
